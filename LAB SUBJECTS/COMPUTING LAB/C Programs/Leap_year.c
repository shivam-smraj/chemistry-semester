/*Classwork 01-02-2021*/
/*Write a program to determine that a input a year is leap year or not.*/
#include<stdio.h>

int main()
{
    int year;
    printf("Enter a year : ");
    scanf("%d", &year );

    if( year % 400)// a%b(Modulo operator) --> Returns the remainder when a/b
}