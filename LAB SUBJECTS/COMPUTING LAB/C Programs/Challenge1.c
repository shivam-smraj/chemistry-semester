#include <stdio.h>//including the header file to include the package which contains prinf() we will use to print our name
int main()//all the codes are witten inside int main() function in a C program
{//start of main()
    printf("My Name is Tathagata Ghosh");//print() is the function which telss the compiler to print what is there within ""s
    return 0;//return statement is compulsory for integer return type of main() function
}//end of main()